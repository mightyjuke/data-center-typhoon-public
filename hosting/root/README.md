# Kevin Mok developer root

These files are prepared for the optional `mightyjuke.github.io` repository. The main website remains in `mightyjuke/data-center-typhoon-public`.

Publish this folder at `https://mightyjuke.github.io/` so AdMob can retrieve the exact seller record from `/app-ads.txt`. Do not use the project-subfolder app-ads.txt URL as a substitute for the hostname-root file.
